def prob_functionator(prb fun input):
fun_list=split into list
for v in list
  add '(*{gen_returns})'
return fun_list
----------
input what to import
input generator function with parenthesis
input problem functions sep with space and no parenthesis
prob_fun_list=prob_fuctionator(prb fun input)

#text 
--
import {imports}
gen_returns={gen}
problem(*{gen_returns})

--

#add to the text file

for i in len fun_list
  add  f"timit.timit('{fun[i]}', number=10000) string to text file #in seperate lines
-------------------------------------------------------------------------
a.b
d.e

from a import x
from d import e
REMAP AND REDO
-



gen problem and save values
input all mod and fun to test














